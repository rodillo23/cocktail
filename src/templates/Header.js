const Header = ()=>{
  const view = `
    <div class="container header-container">
      <nav>
        <img src="assets/img/logo.png"/>
        
      </nav>
    </div>
  `
  return view
}

export default Header